use testing:print;
use testing:boolean;

public int main() {
	println("Hello world!");
	printInteger(5 < 5+5);

	if(5 < 10){
	    println(null);
	} else {
	    println("Hi");
	}

	for(var i = 0; i < 5; i++){
	    var j = 0;
	    j += i;
	    printInteger(j);
	}

	var i = 0;

	while (true){
	    print("While loop: ");
	    printInteger(i);
	    i++;

	    if (i > 5){
	        break;
	    }
	}

	loop {
	    println("Loop loop");

	    break;
	}

	printTestWrapper();

	printBoolean(true);

	printDouble(-5.0e4);

	return 0;
}