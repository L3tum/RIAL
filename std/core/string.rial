struct String{
    public char[] data;
    public uint length;
}

public String to_string(this CString cstr){
    var str = String();
    str.data = cstr;
    str.length = sizeof(cstr) / sizeof(Char);

    return str;
}

public CString to_cstring(this String str){
    return str.data[0];
}